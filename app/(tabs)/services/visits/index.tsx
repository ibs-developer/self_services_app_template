import NavigateHeder from "@/components/reusable/navigationHeader";
import React from "react";
import { View } from "react-native";

const Visits = () => {
  return (
    <View className="flex-1 bg-background">
      <NavigateHeder title="Visits" />
    </View>
  );
};

export default Visits;
